import './globals.css'

export const metadata = {
  title: 'TikTok & Douyin Downloader',
  description: 'Tải video không watermark',
}

export default function RootLayout({ children }) {
  return (
    <html lang="vi">
      <body>{children}</body>
    </html>
  )
}
